# Crie uma variável chamada frase e atribua a ela uma frase qualquer.
# Utilize a técnica de indexação para imprimir somente a primeira palavra da frase.

frase = "A persistência é o caminho do êxito."
primeira_palavra = frase[:frase.index(' ')]
print(primeira_palavra)
