#  Converta a variável “texto” em um número inteiro utilizando a função “int()”
# e atribua o resultado a uma nova variável chamada “numero_convertido”.

texto = '10'
numero_convertido = int(texto)
print(numero_convertido)