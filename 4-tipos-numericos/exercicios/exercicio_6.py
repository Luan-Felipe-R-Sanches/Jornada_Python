# Converta a variável “texto_decimal” em um número decimal utilizando a
# função “float()” e atribua o resultado a uma nova variável chamada
# “decimal_convertido”

texto_decimal = 6.5
decimal_convertido = float(texto_decimal)

print(decimal_convertido)